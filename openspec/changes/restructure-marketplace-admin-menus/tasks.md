## 1. 菜单与权限契约

- [x] 1.1 更新`plugin.yaml`：新增插件市场目录（`type: D`），子菜单“我的插件 / 插件列表 / 插件审核”，隐藏详情；移除或停用旧 catalog/console 可见菜单。
- [x] 1.2 绑定权限：`market:plugin:publish` → 我的插件；`market:plugin:review` → 插件列表与插件审核；详情按进入来源所需权限处理。
- [x] 1.3 更新`manifest/i18n`中英文菜单与页面文案键；提升插件版本以便菜单同步。

## 2. 后端列表与审核查询

- [x] 2.1 扩展市场插件列表查询：支持发布者`scope=mine`（强制归属过滤，`publish`权限）与管理者全量状态列表（`review`权限）。
- [x] 2.2 新增或扩展待审版本队列查询（默认`submitted/reviewing`，支持筛选，批量装配插件名，避免 N+1）。
- [x] 2.3 为待审队列查询补齐必要索引或查询路径说明；确认写接口权限边界（发布者不可 review）。
- [x] 2.4 补充或更新后端单元测试：归属过滤、越权拒绝、待审队列默认过滤。

## 3. 前端页面拆分

- [x] 3.1 实现“我的插件”页：归属列表、状态列、发布/新版本入口（分步或抽屉），表单`rules`与错误提示。
- [x] 3.2 实现“插件列表”页：管理者全量状态、筛选、进详情；无完整发布向导。
- [x] 3.3 实现“插件审核”页：默认待审队列、主从审计区、通过/拒绝决策。
- [x] 3.4 修复详情跳转与返回路由为有效工作台 path；处理 tab 复用，避免重复详情标签。
- [x] 3.5 移除或降级旧`catalog`管理入口与`console`一体页的默认使用路径，清理无效跳转。

## 4. 验证与回归

- [x] 4.1 更新插件 E2E / page object / fixtures 至新菜单 path 与角色场景（发布者仅我的插件；管理者列表+审核）。
- [x] 4.2 本地验证：后端`go test ./backend/...`与插件前端、API 文档 i18n 单测通过；菜单权限差集已在浏览器角色场景确认。
- [x] 4.3 运行前端类型检查、`make lint plugins=true`、`make plugins.check`与`openspec validate restructure-marketplace-admin-menus --strict`。
- [x] 4.4 完整插件市场 E2E 共 5 个子场景通过，并完成 1440、1024、390 视口截图审查。

## Feedback

- [x] **FB-1**: 对齐“我的插件”列表与发布流程的管理台规范，补齐可见性列、响应式操作列、标准抽屉、表单重置和已有发布者选择。由 TC-1a 覆盖角色隔离、1024 紧凑布局、表单校验与重置，TC-1b 覆盖新插件和新版本发布。
- [x] **FB-2**: 修复插件列表、插件审核与详情页的响应式布局、审核状态隔离、空态、风险语义和中英文文案。由 TC-2a 覆盖 Admin/Review 与风险明细，TC-3a 覆盖英文详情、390 布局和本地化错误。
- [x] **FB-3**: 闭环发布者与审核者进入详情、文档和风险检查的权限路径，并验证归属与审核数据边界。由后端 owner/artifact 零副作用测试及 TC-1a、TC-2a、TC-3a、TC-3b 覆盖。
- [x] **FB-4**: 更新插件市场 E2E、POM 与 fixtures，覆盖角色菜单、发布校验、审核队列、详情返回及 1440/1024/390 截图回归。完整模块 TC-1a、TC-1b、TC-2a、TC-3a、TC-3b 共 5/5 通过。
- [x] **FB-5**: 收敛“我的插件”发布工作台：去掉顶部搜索栏；独立“登记发布者”入口维护发布者资料；“发布插件”抽屉仅登记插件基本信息并支持上传包或提交 Git 仓库地址自动识别；移除独立“登记 Git 源”按钮。由 TC-1a/TC-1b、TC004 与截图审查覆盖（端口 19120/15666）。
- [x] **FB-6**: 一个用户只能绑定一个发布者：打开“登记发布者”时回填已有资料并允许编辑保存；禁止再次创建第二份资料；发布者 Key 创建后不可变。由后端单测与 TC-1a 子断言覆盖。
- [x] **FB-7**: 发布者 Key 登记后仍允许修改；更新时校验新 Key 全局唯一，路径用当前 Key 定位、请求体携带目标 Key。由 TC-1a 覆盖可编辑 Key 回填。
- [x] **FB-8**: 将“发布插件”收敛为“添加插件”：添加仅进入“我的插件”私有列表；列表提供“发布”（每次须管理员审核）与“下架”（撤回市场可见，可再次发布并再次审核）。
- [x] **FB-9**: 添加插件的上传包流程仅上传压缩包；服务端自动解压、校验目录规范并解析 plugin.yaml 基本信息，不要求手填 ID/名称/版本/类型。
- [x] **FB-10**: 添加插件的 Git 仓库流程去掉可见性选择；默认私有草稿直至发布审核通过。
- [x] **FB-11**: 统一“添加插件”抽屉中上传包与 Git 仓库模式的左右布局、底部主按钮位置和“添加插件”按钮文案。由插件前端单测、TC-1a、TC004 与 1440 视口截图审查覆盖。
- [x] **FB-12**: 逐页审查「我的插件」「插件列表」「插件审核」的中英文布局与核心交互，以「我的插件」为基准统一筛选区、表格列顺序与固定操作列，修复英文关键表头、审核提交时间截断及固定操作列遮挡关键状态，并由 TC-2a、TC-3a 及 1440/1024/390 截图回归覆盖。
- [x] **FB-13**: 阶段 A：去掉用户可感知的「可见性」设计——「我的插件」与详情不再展示可见性列/标签；公开市场目录以已发布状态为准；审核通过时内部同步 visibility=public；保留底层字段与过滤兼容。由前端单测、TC-1a/TC-3a 与 `openspec validate restructure-marketplace-admin-menus --strict` 覆盖。

### FB-13 影响与验证记录

- `i18n`：无新增键；去掉可见性列/标签展示，既有 `detail.visibility.*` 资源保留未删。
- 缓存一致性：无影响；未改缓存或读模型失效机制（仍走既有 `rebuildPluginReadModel`）。
- 数据权限/可见性：有影响但边界收敛为状态驱动——公开目录仍要求 `market_status=published`，审核通过时同步 `visibility=public`；底层 grant 过滤兼容保留；未删除 API 字段。
- 开发工具跨平台：无影响。
- 运行期依赖与`DI`：无影响。
- 测试：`marketplace-frontend` 单测 10/10；`go test ./backend/internal/service/marketplace/` 通过；E2E TC-1a、TC-3a 通过。

### FB-12 影响与验证记录

- `i18n`有影响：三页在`locale`变化时重建筛选项、列标题与操作文案，`E2E`明确断言中英文实际文案与本地化插件名称，不依赖双语正则作为翻译正确性证据。
- 缓存一致性无影响：页面布局、表格配置与详情弹窗装配不读写缓存或派生快照。
- 数据权限无影响：未修改菜单权限、API 权限、角色范围或数据读写边界；E2E 继续覆盖发布者、审核者与无下载权限角色。
- 开发工具跨平台无影响：未修改开发工具、脚本、runner 或默认执行入口。
- 运行期依赖与`DI`无影响：前端详情弹窗改为既有组件静态导入，未新增后端依赖、构造函数或启动装配。
- 测试策略：`Marketplace`六个`TC`文件共`9/9`通过；已多模态审查中文`1440/1024`、英文三表`1440`及详情、文档、风险、错误态`390`截图，未发现页面级横向溢出、关键表头截断、固定操作列遮挡或原始`i18n key`。
- 已通过前端单测`10/10`、`vue-tsc`、`make lint plugins=1`、`make plugins.check`、`Prettier`、`git diff --check`与`openspec validate restructure-marketplace-admin-menus --strict`。
- 全局`make i18n.check`已在应用当前工作区差异并按`linapro/main`锁定提交`ebbd06be350e1953a02cca39df6559eee0ad09d0`检出完整`official-plugins`的临时工作区通过；此前失败是当前官网检出仅包含`Marketplace`、未包含`linapro-content-notice`自有资源所致，三个宿主引用键在该插件`en-US`与`zh-CN`资源中均已存在。
