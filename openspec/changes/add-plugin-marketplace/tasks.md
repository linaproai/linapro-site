## 1. 插件骨架与边界确认

- [x] 1.1 检查`apps/lina-plugins/linapro-plugin-marketplace/`是否存在插件本地`AGENTS.md`，记录并遵守插件本地规范。
- [x] 1.2 创建`linapro-plugin-marketplace`源码插件目录结构，包括`plugin.yaml`、`plugin_embed.go`、`backend/`、`frontend/`、`manifest/sql/`、`manifest/i18n/`、`manifest/docs/`和`hack/tests/`。
- [x] 1.3 在`plugin.yaml`中声明`id: linapro-plugin-marketplace`、`type: source`、`distribution: builtin`和`i18n.enabled: true`，并维护源码插件注册绑定。
- [x] 1.4 确认市场业务默认闭环在插件目录内；如发现必须修改`lina-core`稳定能力，先补充设计记录和影响分析再实施。

## 2. 数据库与生成模型

- [x] 2.1 设计市场插件`manifest/sql/001-add-plugin-marketplace.sql`，覆盖发布者、插件、版本、产物、文档索引、风险摘要、分类标签、下载会话、下载事件和读模型表。
- [x] 2.2 为`pluginId`唯一查询、发布者列表、版本唯一约束、状态分页、分类标签过滤、文档语言读取、下载会话过期校验和可见性过滤设计索引。
- [x] 2.3 将市场状态、插件类型、审核状态、产物类型、风险类型、可见性和下载会话状态纳入字典或命名类型治理，并维护必要 Seed DML。
- [x] 2.4 运行数据库初始化和插件 DAO 生成流程，确认 SQL 幂等性、PostgreSQL-only、软删除、自增主键写入和生成代码边界合规。

## 3. 后端 API 与服务实现

- [x] 3.1 新增市场插件后端 API DTO，按用途拆分接口文件，所有`g.Meta`、`dc`、`eg`、`permission`和时间字段契约合规。
- [x] 3.2 实现发布者、插件 ID 归属、草稿版本、已发布版本不可变和审核状态流转服务。
- [x] 3.3 实现源码插件`.zip`市场包上传、结构校验、`plugin.yaml`解析、SQL/i18n/docs/依赖摘要和审核失败诊断。
- [x] 3.4 实现动态插件运行时`.zip`市场包上传、`plugin.wasm`解析、根级清单与内嵌清单一致性校验、ABI、`hostServices`、路由、SQL 和资源摘要。
- [x] 3.5 实现`manifest/docs/`文档索引、语言回退、图片路径限制、安全渲染和搜索摘要提取。
- [x] 3.6 实现市场列表读模型、分页查询、最小投影、详情、版本、风险和文档接口，避免`N+1`查询和前端逐项补查。
- [x] 3.7 实现公开、私有和预留授权可见性过滤，在列表、详情、文档、风险、下载和聚合统计查询阶段注入数据权限。
- [x] 3.8 实现下载会话、短期地址或受控下载读取、`sha256`校验和、过期拒绝、下载事件和异步统计快照。
- [x] 3.9 实现市场插件业务错误码、英文 fallback、`messageKey`、日志上下文和初始化依赖显式注入。

## 4. 前端页面与多语言

- [x] 4.1 新增市场前端 API 适配层和类型定义，路径以当前`apps/lina-vben/apps/web-antd`结构及插件前端装配机制为准。
- [x] 4.2 实现市场目录列表、筛选、分页、风险摘要、发布者标记和兼容版本展示，使用`Page`和`useVbenVxeGrid`。
- [x] 4.3 实现插件详情、版本列表、文档浏览、风险摘要和下载确认流程。
- [x] 4.4 实现发布者草稿上传、提交审核、审核结果查看，以及审核后台的清单一致性、SQL、`hostServices`和文档入口检查视图。
- [x] 4.5 实现动态插件下载后提取`plugin.wasm`并复用现有动态上传治理的前端衔接；源码插件下载流程不得展示运行时一键安装。
- [x] 4.6 维护市场插件运行时语言包、菜单/路由文案、错误文案和`apidoc`翻译资源，确认前端不在模块顶层调用`$t()`求值。

## 5. 单元测试与集成验证

- [x] 5.1 覆盖源码市场包结构校验、动态运行时包清单一致性、开发源码目录拒绝和版本不可覆盖的后端单元测试。
- [x] 5.2 覆盖`manifest/docs/`语言回退、安全渲染、越界图片拒绝和文档缓存键的后端单元测试。
- [x] 5.3 覆盖市场列表读模型、分页上限、可见性过滤、下载权限拒绝、下载会话过期和统计快照的后端测试。
- [x] 5.4 覆盖市场前端 API 适配、核心组合逻辑、i18n key 覆盖和动态导入衔接的前端单元测试或组件测试。
- [x] 5.5 运行覆盖市场插件后端包的`go test <changed-package> -count=1`，涉及路由绑定时运行插件或宿主启动绑定测试。
- [x] 5.6 运行前端类型检查、相关测试和`make i18n.check`或等价 i18n 治理验证。

## 6. E2E 用例

- [x] 6.1 使用`lina-e2e`规范创建`apps/lina-plugins/linapro-plugin-marketplace/hack/tests/e2e/TC001-marketplace-catalog.ts`，覆盖市场列表、筛选、详情、文档和风险摘要。
- [x] 6.2 创建`apps/lina-plugins/linapro-plugin-marketplace/hack/tests/e2e/TC002-publish-review-download.ts`，覆盖源码包上传审核、动态包上传审核、下载会话和动态上传治理衔接。
- [x] 6.3 创建`apps/lina-plugins/linapro-plugin-marketplace/hack/tests/e2e/TC003-visibility-i18n.ts`，覆盖私有可见性过滤、下载权限拒绝和关键中文/英文文案不显示原始 i18n key。
- [x] 6.4 为市场插件专属页面对象和 helper 放入`apps/lina-plugins/linapro-plugin-marketplace/hack/tests/pages/`和`hack/tests/support/`，不得回流到宿主共享测试目录。
- [x] 6.5 执行市场插件 E2E，并将截图、日志和中间文件保存到项目`temp/`目录下符合规范的位置。

## 7. 治理校验与审查

- [x] 7.1 运行`openspec validate add-plugin-marketplace --strict`。
- [x] 7.2 记录架构边界、数据权限、缓存一致性、i18n、开发工具跨平台、SQL 幂等性、测试策略和 DI 来源检查结果。
- [x] 7.3 检查`apps/lina-core/pkg/plugin`README 是否因插件能力或宿主公共契约变化需要同步更新；若确认无影响，记录无影响判断。
- [x] 7.4 完成实现和验证后调用`lina-review`进行代码与规范审查，审查通过后再标记任务完成。

## 执行记录

- `1.1`：已检查`apps/lina-plugins/linapro-plugin-marketplace/AGENTS.md`，当前不存在插件本地规范文件，继续遵守项目顶层`AGENTS.md`和命中规则文件。
- `1.2`：已创建市场插件源码目录、独立`go.mod`、`backend/`、`frontend/`、`manifest/sql/`、`manifest/i18n/`、`manifest/docs/`、`hack/tests/`、双语`README`和插件本地`hack/config.yaml`；为使新增插件可被主仓库跟踪，已通过既有跨平台入口`go run . plugins.init`将`apps/lina-plugins`从`gitlink`转换为普通目录并保留文件。
- `1.3`：`plugin.yaml`已声明`id: linapro-plugin-marketplace`、`type: source`、`distribution: builtin`、`scope_nature: platform_only`、`supports_multi_tenant: false`、`default_install_mode: global`和`i18n.enabled: true`；源码注册绑定位于`backend/plugin.go`，根部`plugin_embed.go`提供嵌入资源，符合当前`linactl`聚合入口会空白导入源码插件`backend`包的构建方式。
- `1.4`：当前市场业务仅新增`apps/lina-plugins/linapro-plugin-marketplace/`内文件，并修正本变更设计文档中的源码插件 API 命名空间描述；未修改`apps/lina-core`稳定插件能力或公共契约。缓存、数据权限、SQL 和前端行为尚未进入运行期实现阶段，后续任务分别验证。
- 验证：已运行`GOWORK=off go test ./... -count=1`，覆盖插件根包和`backend`包静态注册测试；直接`go test ./...`会被根`go.work`排除该新插件模块，后续完整插件模式验证应使用临时插件工作区或插件构建入口。
- `2.1`：已新增`apps/lina-plugins/linapro-plugin-marketplace/manifest/sql/001-add-plugin-marketplace.sql`，包含发布者、插件主记录、版本、产物、文档索引、风险摘要、分类标签、可见性授权、下载会话、下载事件和列表读模型表；所有表使用`CREATE TABLE IF NOT EXISTS`和`TIMESTAMPTZ`时间字段，插件业务数据保留`deleted_at`软删除边界，下载事件作为流水表不设置软删除。
- `2.2`：已覆盖`plugin_id`唯一查询、发布者状态列表、`plugin_id + release_version`版本唯一约束、市场状态分页、插件类型状态过滤、分类标签过滤、文档语言读取、下载会话过期校验、请求人会话查询、可见性授权过滤和读模型分页索引；列表接口后续应读取`plugin_marketplace_plugin_read_model`最小投影和标签关系，避免逐项解析包或读取完整`Markdown`。
- `2.3`：已维护`plugin_marketplace_plugin_type`、`plugin_marketplace_status`、`plugin_marketplace_review_status`、`plugin_marketplace_artifact_type`、`plugin_marketplace_risk_type`、`plugin_marketplace_visibility`和`plugin_marketplace_download_session_status`字典类型及 Seed DML；Seed 不写入自增`id`，使用现有唯一约束配合`ON CONFLICT DO NOTHING`保证幂等。
- SQL 验证：已运行静态检索确认无`ON DUPLICATE`、`AUTO_INCREMENT`、`SERIAL`、`DROP TABLE`或显式写入`"id"`；已运行`go test ./pkg/dialect -run TestPostgreSQLProjectSQLUsesTimestamptzForInstants -count=1`验证项目 SQL 时间字段治理；该测试原先硬编码旧官方插件字段，已修正为通用插件 SQL 裸`TIMESTAMP`扫描，避免新增插件 SQL 被旧业务字段耦合误报。
- `2.4`：已创建被`.gitignore`忽略的本地`apps/lina-core/manifest/config/config.yaml`用于数据库初始化，内容来自`config.template.yaml`；`linactl env.check`确认本机`PostgreSQL 18.3.0`可用。已运行`go run . db.init confirm=init`完成宿主数据库初始化；随后运行`go run . build dir=apps/lina-plugins/linapro-plugin-marketplace plugins=1`生成插件完整`go.work`与`lina-plugins`聚合模块，并在`GOWORK=temp/go.work.plugins GOFLAGS=-tags=official_plugins`模式下启动宿主触发`builtin`源码插件收敛，日志显示`linapro-plugin-marketplace`执行`plugin_sql_install`。该宿主进程在启动完成后因`:9120`已被占用退出，未影响插件 SQL 已执行的验证结论。
- DAO 生成验证：已运行`go run . dao dir=apps/lina-plugins/linapro-plugin-marketplace/backend`，生成`backend/internal/dao`、`backend/internal/model/do`和`backend/internal/model/entity`下 12 张`plugin_marketplace_*`表的 48 个 GoFrame 生成文件；生成边界静态检查确认未生成`sys_*`宿主表工件，生成文件未手工修改。已移除不再需要的`backend/internal/.gitkeep`占位文件。
- `2.4`治理验证：已重新静态检索`manifest/sql/001-add-plugin-marketplace.sql`，确认无`ON DUPLICATE`、`AUTO_INCREMENT`、`SERIAL`、`DROP TABLE`、裸`TIMESTAMP`或显式自增`id`写入；SQL 仍为`CREATE ... IF NOT EXISTS`、`ON CONFLICT DO NOTHING`和`TIMESTAMPTZ`的`PostgreSQL`幂等实现。软删除边界保持为业务主表、草稿和授权类表使用`deleted_at`，下载事件流水不使用软删除。已运行`GOWORK=off go test ./... -count=1`覆盖市场插件根包、`backend`注册测试及生成包编译。
- `3.1`：已新增`apps/lina-plugins/linapro-plugin-marketplace/backend/api/market/v1/`下市场后端 API DTO，按公共投影、目录查询、文档风险、发布审核和下载会话拆分文件；路由路径使用插件内部`/market/...`相对路径，后续通过源码插件注册器挂载到公开前缀`/x/linapro-plugin-marketplace`。查询接口使用`GET`并设置分页上限，创建、上传和下载会话使用`POST`，状态和审核更新使用`PUT`；所有请求字段使用`json`标签，`g.Meta`已声明`dc`、`summary`、`tags`和`permission`，响应时间点字段均为`int64`或`*int64`的 Unix 毫秒时间戳。
- `3.1`接口治理记录：API DTO 未引入运行期 service/controller 依赖，`DI`来源检查结论为无新增运行期依赖；缓存一致性无新增运行期缓存实现，后续读模型、文档缓存和统计快照在对应服务任务中验证。数据权限影响已体现在接口契约说明中，列表、详情、文档、风险和下载会话均要求可见性过滤，实际查询阶段注入在`3.6`、`3.7`和`3.8`实现。开发工具跨平台无影响，未修改`Makefile`、`linactl`或脚本。`i18n`有影响，已保持 API 源文本为英文，并在插件自己的`manifest/i18n/zh-CN/apidoc/plugin-marketplace.json`补充`plugins.linapro_plugin_marketplace.api.market.v1.*`中文接口文档翻译；`en-US`接口文档资源继续为空对象占位。
- `3.1`验证：已运行`jq empty apps/lina-plugins/linapro-plugin-marketplace/manifest/i18n/zh-CN/apidoc/plugin-marketplace.json`验证翻译 JSON；已静态检索确认新增 DTO 无`p`标签、无公开响应`time.Time`或`gtime.Time`；已运行`GOWORK=off go test ./... -count=1`覆盖市场插件根包、`backend`包、`backend/api/market/v1`及生成包编译；已运行`GOWORK=/Users/john/Workspace/github/linaproai/linapro-site/temp/go.work.plugins GOFLAGS=-tags=official_plugins go test ./internal/service/apidoc -run 'TestOpenAPIMetadataUsesEnglishSourceText|TestOpenAPII18nBundlesCoverCurrentMetadata|TestOpenAPIBundlesAreSeparatedFromRuntimeI18n' -count=1`验证英文源文本、插件`apidoc`翻译完整性和运行时语言包隔离；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `3.2`：已新增`apps/lina-plugins/linapro-plugin-marketplace/backend/internal/service/marketplace/`服务组件，包含发布者创建、发布者可用性校验、插件 ID 稳定归属、插件草稿元数据保存、版本草稿保存与替换、提交审核、审核通过或拒绝、插件生命周期状态更新和服务自有投影结构。服务返回`PublisherRecord`、`PluginRecord`和`ReleaseRecord`，未向调用方泄漏生成的`DAO`、`DO`或`Entity`结构。
- `3.2`状态流转记录：`SavePluginDraft`要求发布者存在且处于`active`，同一`pluginId`已归属其他发布者时返回`CodeMarketplacePluginIDOwned`；`SaveReleaseDraft`仅允许创建新草稿或在`ReplaceDraft=true`时替换仍可变的草稿或拒绝版本，已提交、审核中、已批准、已发布、已下架或已废弃版本均拒绝覆盖；拒绝版本重新替换草稿时，因 GoFrame 生成`DO`会忽略`nil`指针字段，已在同一事务内使用显式列名`nil`清空旧`submitted_at`、`reviewed_at`和`published_at`业务时间。`ReviewRelease`审核通过时在同一事务内发布版本并更新插件`latest_release_id`、`latest_version`和首次`published_at`，审核拒绝时保留版本为可继续修改的草稿生命周期并记录拒绝消息。
- `3.2`治理记录：数据库写入和查询条件优先使用插件自身生成的`DO`对象，未使用`g.Map`、`map[string]interface{}`或`map[string]any`写库；未手动写入`created_at`、`updated_at`或`deleted_at`，也未添加`WhereNull(deleted_at)`。`submitted_at`、`reviewed_at`和`published_at`为业务审核时间，不属于 GoFrame 自动维护字段。`DI`来源检查结论为本服务当前无新增运行期依赖，`New()`不创建缓存、权限、`i18n`、存储或其他关键服务独立实例；后续包存储、缓存和权限服务如进入本组件，必须改为构造函数显式注入。缓存一致性无新增运行期缓存实现，读模型、文档缓存和下载统计快照仍由后续任务验证。数据权限当前实现发布者与插件 ID 归属校验，公开、私有和授权可见性查询过滤仍在`3.7`实现。开发工具跨平台无影响，未修改构建脚本或`linactl`入口。`i18n`有影响，已新增市场业务错误码英文 fallback，并在插件自身`manifest/i18n/en-US/plugin.json`和`manifest/i18n/zh-CN/plugin.json`维护运行时错误翻译。
- `3.2`验证：已运行`gofmt -w apps/lina-plugins/linapro-plugin-marketplace/backend/internal/service/marketplace/marketplace_release.go`；已静态检索确认市场服务代码无`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`、`errors.New`、`fmt.Errorf`或`_ =`，仅保留服务投影中的`UpdatedAt`读取；已运行`GOWORK=off go test ./... -count=1`覆盖市场插件根包、`backend`包、`backend/internal/service/marketplace`及生成包编译；已运行`jq empty manifest/i18n/en-US/plugin.json manifest/i18n/zh-CN/plugin.json manifest/i18n/zh-CN/apidoc/plugin-marketplace.json manifest/i18n/en-US/apidoc/plugin-marketplace.json`验证插件多语言 JSON；已运行`GOWORK=/Users/john/Workspace/github/linaproai/linapro-site/temp/go.work.plugins GOFLAGS=-tags=official_plugins go test ./internal/service/apidoc -run 'TestOpenAPIMetadataUsesEnglishSourceText|TestOpenAPII18nBundlesCoverCurrentMetadata|TestOpenAPIBundlesAreSeparatedFromRuntimeI18n' -count=1`验证插件`apidoc`资源仍合规；已运行`openspec validate add-plugin-marketplace --strict`并通过。版本不可覆盖等行为单元测试按`5.1`任务集中补齐。
- `3.3`：已在`marketplace.Service`新增`UploadSourcePackage`，实现源码插件`.zip`市场包扫描和草稿落库。新增`marketplace_source_package.go`负责读取 controller 后续提供的服务器本地`PackagePath`，校验 ZIP 安全路径、支持直接根目录或单一插件根目录、拒绝路径穿越和重复文件、要求`plugin.yaml`、`go.mod`、`backend/plugin.go`、`frontend/`、`manifest/`、`plugin_embed.go`和至少一个`manifest/docs/**/*.md`文档入口；解析`plugin.yaml`的插件 ID、版本、类型、分发模式、租户治理、`i18n`和依赖声明，校验源码包`type=source`、插件 ID 命名、版本语义、上传字段与清单一致性、框架依赖和插件依赖格式。
- `3.3`摘要与 artifact 记录：源码包扫描会生成`manifest_snapshot`、`dependency_summary`、`sql_summary`、`i18n_summary`、`docs_summary`和`risk_summary`JSON，计算整包`sha256`与`plugin.yaml`校验和，并通过`SaveReleaseDraft`保存或替换仍可变的版本草稿；随后在同一事务内写入或更新`plugin_marketplace_artifact`的`source_zip`元数据。未在本任务引入物理对象存储服务，`StorageKey`由后续 controller/storage 层传入或按`source/<pluginId>/<version>/<sha>.zip`派生；实际受控下载读取仍由`3.8`实现。当前不写入风险明细表和文档索引表，完整风险展示与文档索引由`3.5`和后续风险任务补齐。
- `3.3`错误与`i18n`记录：已新增`CodeMarketplacePackageInvalid`、`CodeMarketplacePackageStructureInvalid`、`CodeMarketplacePackageManifestMismatch`和`CodeMarketplacePackageScanFailed`业务错误码，源码包无效、结构缺失、`plugin.yaml`与上传字段不一致时返回稳定错误码和`diagnostic`参数；不可读的服务器临时包路径不会回显本机文件路径。已同步维护插件自身`manifest/i18n/en-US/plugin.json`和`manifest/i18n/zh-CN/plugin.json`错误翻译。新增直接导入`gopkg.in/yaml.v3`用于插件内闭环解析，已运行`GOWORK=off go mod tidy`将其提升为直接依赖；该依赖原本已由模块间接引入，未新增外部运行期服务。
- `3.3`治理记录：源码包解析未导入`apps/lina-core/internal/...`，遵守 Go `internal`边界并保持市场业务闭环在源码插件目录；数据库写入仍使用插件自身`DO`对象，未使用`g.Map`或`map[string]interface{}`写库。`DI`来源检查结论为无新增运行期服务依赖，`UploadSourcePackage`只使用本地文件路径、`yaml`解析和已有`DAO`；后续如果接入对象存储、权限、缓存或审计服务，需要通过构造函数显式注入。缓存一致性无新增运行期缓存实现；数据权限当前复用发布者和插件 ID 归属校验，公开/私有可见性过滤仍在`3.7`实现；开发工具跨平台无影响，未修改脚本、`Makefile`或`linactl`入口。新增测试为后续`5.1`提供源码包结构校验覆盖的一部分，但`5.1`仍需补齐动态运行时包和版本不可覆盖等完整测试后再标记。
- `3.3`验证：已运行`gofmt`格式化新增服务和测试文件；已运行`jq empty apps/lina-plugins/linapro-plugin-marketplace/manifest/i18n/en-US/plugin.json apps/lina-plugins/linapro-plugin-marketplace/manifest/i18n/zh-CN/plugin.json apps/lina-plugins/linapro-plugin-marketplace/manifest/i18n/zh-CN/apidoc/plugin-marketplace.json apps/lina-plugins/linapro-plugin-marketplace/manifest/i18n/en-US/apidoc/plugin-marketplace.json`验证插件多语言 JSON；已新增并运行`GOWORK=off go test ./backend/internal/service/marketplace -run TestScanSourcePackage -count=1`覆盖合法单根源码包、ZIP 路径穿越拒绝和`plugin.yaml`版本不一致拒绝；已运行`GOWORK=off go test ./... -count=1`覆盖插件根包、`backend`包、市场服务包和生成包编译；已静态检索确认市场服务代码无`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`、`errors.New`、`fmt.Errorf`或`_ =`，仅保留`3.2`记录过的显式可空审核时间字段清空和服务投影`UpdatedAt`读取；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `3.4`：已在`marketplace.Service`新增`UploadDynamicPackage`，实现动态插件运行时`.zip`市场包扫描和草稿落库。新增`marketplace_dynamic_package.go`负责校验 ZIP 安全路径、支持直接根目录或单一插件根目录、要求根级`plugin.yaml`和`plugin.wasm`、拒绝`backend/`、`frontend/`、`hack/`和`main.go`等开发源码目录；解析根级`plugin.yaml`并要求`type=dynamic`、`distribution=managed`、插件 ID、版本、依赖和上传字段一致。`plugin.wasm`解析通过`lina-core/pkg/plugin/pluginbridge/protocol`公开的`ListCustomSections`和段名常量完成，未导入宿主`internal`包；已校验内嵌清单、运行时`ABI`、`runtimeKind=wasm`、`hostServices`、动态路由、桥接契约、SQL、运行时`i18n`、`apidoc i18n`和`manifest`资源段，并比对根级清单与内嵌清单的`id`、`name`、`version`、`type`、依赖、`hostServices`和多租户字段一致性。
- `3.4`摘要与 artifact 记录：动态包扫描会生成`manifest_snapshot`、`dependency_summary`、`host_service_summary`、`route_summary`、`sql_summary`、`i18n_summary`、`docs_summary`和`risk_summary`JSON；`manifest_snapshot`包含根级清单、内嵌清单、运行时元数据、桥接契约和内嵌`manifest`资源摘要。扫描完成后通过`SaveReleaseDraft`保存或替换仍可变的动态版本草稿，并在同一事务内写入或更新`dynamic_zip`和`plugin_wasm`两类`plugin_marketplace_artifact`元数据，记录整包、根清单和`plugin.wasm`校验和。当前仍不写入风险明细表和文档索引表，完整文档索引和风险展示由`3.5`及后续任务补齐。
- `3.4`治理记录：动态包解析保持市场业务闭环在`apps/lina-plugins/linapro-plugin-marketplace/`内，仅依赖宿主已公开的`pluginbridge`协议与契约校验函数，未修改`apps/lina-core`稳定能力或公共契约。数据库写入继续使用插件自身生成的`DO`对象，未使用`g.Map`或`map[string]interface{}`写库，未手动维护`created_at`、`updated_at`或`deleted_at`。`DI`来源检查结论为无新增运行期服务依赖，`UploadDynamicPackage`只使用服务器本地上传路径、ZIP/WASM/JSON/YAML解析和已有`DAO`；对象存储、权限、缓存或审计服务尚未接入，后续接入时必须通过构造函数显式注入。缓存一致性无新增运行期缓存实现；数据权限当前仍只校验发布者和插件 ID 归属，公开、私有和授权可见性过滤由`3.7`实现；开发工具跨平台无影响，未修改脚本、`Makefile`或`linactl`入口。`i18n`资源无新增错误码或 API 文档源文本，运行时错误仍复用`3.3`新增的包校验错误码。
- `3.4`验证：已运行`gofmt`格式化新增服务和测试文件；已新增并运行`GOWORK=off go test ./backend/internal/service/marketplace -run TestScanDynamicPackage -count=1`覆盖合法动态运行时包、动态包携带开发源码目录拒绝和根级清单与内嵌清单版本不一致拒绝；已运行`GOWORK=off go test ./backend/internal/service/marketplace -count=1`和`GOWORK=off go test ./... -count=1`覆盖市场服务包、插件根包、`backend`包、API 包及生成包编译；已运行`jq empty manifest/i18n/en-US/plugin.json manifest/i18n/zh-CN/plugin.json manifest/i18n/zh-CN/apidoc/plugin-marketplace.json manifest/i18n/en-US/apidoc/plugin-marketplace.json`验证插件多语言 JSON；已静态检索确认市场服务代码无`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`、`errors.New`、`fmt.Errorf`或`_ =`；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `3.5`：已新增`marketplace_document.go`，实现市场文档索引构建、语言回退选择、图片路径限制、安全渲染和搜索摘要提取。源码包和动态包上传扫描均在保存版本草稿和 artifact 后调用`replaceReleaseDocuments`写入`plugin_marketplace_doc`索引；索引内容包括`locale`、`doc_path`、`source_kind`、标题、摘要、内容 hash 和搜索文本，不在索引表中存储完整正文。`ResolveReleaseDocumentIndex`按当前语言、插件默认语言、`zh-CN`、`en-US`、`default`和`README.zh-CN.md`、`README.md`顺序选择索引行；后续文档正文接口需要结合产物存储读取正文并复用该索引元数据。
- `3.5`安全与 SQL 记录：文档渲染采用保守的基础标题/段落 HTML 生成，所有原始 HTML 均转义输出，脚本不会作为可执行标签进入响应；Markdown 和 HTML 图片引用在索引阶段校验，拒绝外链、绝对路径、驱动器路径和`../`越界，只允许图片停留在`manifest/docs/assets/`或当前文档目录边界内。为支持草稿重传后安全重建文档索引，已将`uk_plugin_marketplace_doc_release_locale_path`改为`WHERE deleted_at IS NULL`的部分唯一索引，并保留`DROP INDEX IF EXISTS`加`CREATE UNIQUE INDEX IF NOT EXISTS`的幂等迁移方式。
- `3.5`治理记录：文档索引仍闭环在市场源码插件目录，未修改宿主插件公共契约。数据库写入使用插件自身生成的`DO`对象，未手动维护`created_at`、`updated_at`或`deleted_at`；过期文档索引使用`Delete()`软删除，部分唯一索引避免软删除行阻塞重新索引。`DI`来源检查结论为无新增运行期服务依赖，当前仅使用本地包扫描内容、Go 标准库渲染和已有`DAO`；未引入缓存服务，缓存一致性影响仅记录为未来文档缓存键应包含`release_id + locale + path + content_hash`。数据权限可见性过滤仍在`3.7`接入，当前文档索引解析只按版本内部索引选择，不作为公开读取授权依据。`i18n`有影响，已新增文档无效和文档不存在错误码的英文 fallback 与中英文运行时翻译；未新增 API DTO 文档源文本。开发工具跨平台无影响，未修改脚本、`Makefile`或`linactl`入口。
- `3.5`验证：已运行`gofmt`格式化新增服务和测试文件；已新增并运行`GOWORK=off go test ./backend/internal/service/marketplace -run TestIndexMarketplaceDocument -count=1`覆盖脚本标签转义和图片路径越界拒绝；已运行`GOWORK=off go test ./backend/internal/service/marketplace -run 'TestSelectMarketplaceDocumentFallback|TestScanSourcePackage|TestScanDynamicPackage' -count=1`覆盖语言回退选择及源码/动态包扫描接入文档索引后的回归；已运行`GOWORK=off go test ./backend/internal/service/marketplace -count=1`和`GOWORK=off go test ./... -count=1`覆盖插件后端编译；已运行`jq empty manifest/i18n/en-US/plugin.json manifest/i18n/zh-CN/plugin.json manifest/i18n/zh-CN/apidoc/plugin-marketplace.json manifest/i18n/en-US/apidoc/plugin-marketplace.json`验证多语言 JSON；已静态检索确认市场服务代码无`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`、`errors.New`、`fmt.Errorf`或`_ =`，SQL 静态检索确认无`ON DUPLICATE`、`AUTO_INCREMENT`、`SERIAL`、`DROP TABLE`、裸`TIMESTAMP`或显式自增`id`写入；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `3.6`：已新增`marketplace_query.go`实现市场目录、详情、版本、风险和文档的服务层读接口。目录列表读取`plugin_marketplace_plugin_read_model`最小投影并强制分页上限，发布者 key 和标签筛选分别通过发布者表、插件标签关系表做一次性前置解析；当前页发布者信息通过`publisher_id`批量读取，避免列表逐行详情查询、逐项 artifact 解析或逐项 Markdown 读取。版本列表在数据库侧按插件、状态、审核状态分页，当前页 release 的主 artifact 使用`release_id IN (...)`批量装配；详情页固定读取插件、发布者、标签、最新 release 和主 artifact，不随列表规模增长。
- `3.6`读模型和风险记录：审核通过发布版本、插件下架/恢复/废弃状态变更后会在同一事务上下文调用`rebuildPluginReadModel`刷新单个插件读模型，读模型快照包含发布者、最新版本、兼容范围、标签、风险计数、下载统计和搜索文本。源码包和动态包上传扫描在保存文档索引后调用`replaceReleaseRisks`重建`plugin_marketplace_risk`明细，风险接口从风险表分页读取并返回 scanner payload。文档读取接口复用`ResolveReleaseDocumentIndex`的语言回退和索引元数据，当前未引入对象存储服务，响应内容使用已索引安全搜索文本生成保守 HTML；完整原文内容读取仍需后续下载/受控存储能力接入。
- `3.6`治理记录：本任务仍闭环在`apps/lina-plugins/linapro-plugin-marketplace/`源码插件目录，未修改`apps/lina-core`稳定插件能力或公共契约。数据库写入继续使用插件自身生成的`DO`对象，未使用`g.Map`、`map[string]interface{}`或`map[string]any`作为写库载体；过期风险行使用`Delete()`软删除，未手写`created_at`、`updated_at`或`deleted_at`。数据权限影响：目录和版本读接口已限定普通目录以`published`状态为基础边界，但公开、私有、预留授权的当前用户可见性过滤仍按任务拆分在`3.7`注入查询阶段。缓存影响：未新增运行期缓存服务，读模型权威源为市场业务表，发布和状态变化同步刷新单插件读模型；文档缓存后续仍应使用`release_id + locale + path + content_hash`键。`i18n`影响：本任务未新增 API 源文本、运行时文案或错误码，文档和风险接口复用`3.1`/`3.5`已有 DTO 与错误翻译资源。`DI`来源检查结论为无新增运行期服务依赖，`New()`仍不构造缓存、权限、存储或`i18n`服务；后续接入对象存储、数据权限或缓存服务时必须改为构造函数显式注入。开发工具跨平台无影响，未修改`Makefile`、`linactl`或脚本。
- `3.6`验证：已运行`gofmt`格式化新增和修改的 Go 文件；已新增`marketplace_query_test.go`覆盖分页上限、读模型 JSON 快照投影、主 artifact 选择、风险 payload 解析、风险类型分类和文档索引内容转义。已运行`GOWORK=off go test ./backend/internal/service/marketplace -count=1`和`GOWORK=off go test ./... -count=1`覆盖市场服务包、插件根包、`backend`包、API 包、生成 DAO/DO/Entity 包编译。已运行`jq empty manifest/i18n/en-US/plugin.json manifest/i18n/zh-CN/plugin.json manifest/i18n/en-US/apidoc/plugin-marketplace.json manifest/i18n/zh-CN/apidoc/plugin-marketplace.json`验证多语言 JSON；已静态检索确认市场服务代码无`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`、`errors.New`、`fmt.Errorf`或`_ =`，SQL 静态检索确认无`ON DUPLICATE`、`AUTO_INCREMENT`、`SERIAL`、`DROP TABLE`、裸`TIMESTAMP`或显式自增`id`写入。
- `3.7`：已新增`VisibilitySubject`服务输入和`marketplace_visibility.go`可见性过滤组件。列表、详情、版本、文档和风险读取均在数据库查询阶段注入公开、私有和预留授权过滤：`public`记录直接可见，`private`和`reserved`记录必须命中`plugin_marketplace_visibility_grant`中的有效授权子查询；授权范围支持`tenant`、`org`、`user`和未来`reserved_license`，授权行要求`status=1`且未过期，权限区分`view`与后续下载会话可复用的`download`。零值调用主体只匹配公开市场记录，避免 controller 尚未注入身份时误放开私有数据。
- `3.7`读模型与泄露治理：`ListPlugins`对`plugin_marketplace_plugin_read_model`按`plugin_record_id`注入可见性子查询，列表总数、风险计数和下载统计快照仅来自可见记录；`GetPluginDetail`先过滤插件主记录，再按相同主体过滤最新发布版本，不返回不可见版本的版本号、产物、风险或统计信息；`ListReleases`、`GetReleaseDocument`和`ListReleaseRisks`均先确认插件与版本同时可见，不存在或不可见统一返回 not found 类业务错误。读模型刷新时写入插件主记录与最新版本组合后的有效可见性，避免公开插件挂载私有或预留最新版本时在目录投影中泄露。
- `3.7`治理记录：本任务仍闭环在`apps/lina-plugins/linapro-plugin-marketplace/`源码插件目录，未修改`apps/lina-core`稳定插件能力或公共契约。数据库查询阶段使用`IN (subquery)`授权过滤，不先查出范围外数据再内存过滤；未新增数据库迁移，沿用`2.2`已经为可见性授权设计的索引。未新增运行期依赖或服务构造参数，`DI`来源检查结论为无新增运行期依赖；未新增运行期缓存，权限和可见性不依赖缓存放行。`i18n`无新增影响，本任务未修改 API 源文本、运行时文案、错误码或翻译资源；开发工具跨平台无影响，未修改脚本、`Makefile`或`linactl`入口。
- `3.7`验证：已运行`gofmt`格式化新增和修改的 Go 文件；已新增`marketplace_visibility_test.go`覆盖主体范围归一化、零值主体仅公开、授权条件拼装、`view/download`权限归一化和插件/版本组合可见性。已运行`GOWORK=off go test ./backend/internal/service/marketplace -count=1`和`GOWORK=off go test ./... -count=1`覆盖市场服务包、插件根包、`backend`包、API 包、生成 DAO/DO/Entity 包编译。已运行`jq empty manifest/i18n/en-US/plugin.json manifest/i18n/zh-CN/plugin.json manifest/i18n/en-US/apidoc/plugin-marketplace.json manifest/i18n/zh-CN/apidoc/plugin-marketplace.json`验证多语言 JSON；已静态检索确认市场服务代码无`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`、`errors.New`、`fmt.Errorf`或`_ =`，SQL 静态检索确认无`ON DUPLICATE`、`AUTO_INCREMENT`、`SERIAL`、`DROP TABLE`、裸`TIMESTAMP`或显式自增`id`写入；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `3.8`：已新增`marketplace_download.go`和`marketplace_download_test.go`，在`marketplace.Service`中提供`CreateDownloadSession`、`GetDownloadSession`、`RecordDownloadEvent`和`RefreshDownloadStatistics`。创建会话会先按`download`权限调用`requireVisibleRelease`，选择指定产物或版本主产物，绑定请求人、版本、产物、过期时间、授权快照、产物大小和`sha256`，生成`mpdl_`前缀的不透明会话 ID，并返回`/x/linapro-plugin-marketplace/market/download-sessions/{sessionId}/content`受控短期地址。会话默认有效期为`15m`，调用方传入值最多截断到`2h`。
- `3.8`事件与统计记录：会话创建与`created`下载事件在同一事务内写入；`started`、`completed`和`failed`事件必须绑定请求人拥有的有效会话，且过期、撤销、未知状态或无当前下载权限均拒绝。`completed`事件在事务内通过`status=active`和`expires_at > now`条件消费会话，避免并发重复完成导致重复计数；已消费会话仍可读取元数据，但不再返回可用下载地址。`RefreshDownloadStatistics`只从`completed`事件聚合并更新插件主表和读模型`download_count`快照，普通列表、详情和文档读取不产生业务写入。
- `3.8`治理记录：本任务继续闭环在市场源码插件目录，未修改`apps/lina-core`稳定插件能力或公共契约。数据库写入均使用插件自身生成的`DO`对象，未使用`g.Map`、`map[string]interface{}`或`map[string]any`写库，未手动维护`created_at`、`updated_at`或`deleted_at`；`consumed_at`和`expires_at`为业务时间字段。数据权限已在创建会话、读取会话和记录事件前按`download`权限重新校验，且请求人 ID 为必填，不能通过会话内 owner 反推权限。缓存一致性无新增运行期缓存服务；下载统计权威源为`plugin_marketplace_download_event`流水，快照允许异步延迟且不作为权限依据。`DI`来源检查结论为无新增运行期依赖，`New()`未创建对象存储、缓存、权限、`i18n`或审计服务；当前实现返回受控内容路径，后续 controller 或存储读取接入该路径时必须复用会话校验和事件记录。`i18n`有影响，已新增下载产物不存在、会话不存在、会话过期和会话不可用业务错误码英文 fallback，并维护插件自身中英文运行时错误翻译；未新增 API DTO 源文本之外的`apidoc`资源缺口。开发工具跨平台无影响，未修改`Makefile`、`linactl`或脚本。
- `3.8`验证：已运行`gofmt`格式化下载服务和测试文件；已新增下载会话 helper 单元测试，覆盖`TTL`边界、受控下载地址投影、已消费会话不返回下载地址、事件类型默认值、活跃未过期判定、可读元数据状态、授权快照 JSON 和下载产物类型校验。已运行`GOWORK=off go test ./backend/internal/service/marketplace -count=1`和`GOWORK=off go test ./... -count=1`覆盖市场服务包、插件根包、`backend`包、API 包、生成 DAO/DO/Entity 包编译。已运行`jq empty manifest/i18n/en-US/plugin.json manifest/i18n/zh-CN/plugin.json manifest/i18n/en-US/apidoc/plugin-marketplace.json manifest/i18n/zh-CN/apidoc/plugin-marketplace.json`验证多语言 JSON；已静态检索确认市场服务代码无`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`、`errors.New`、`fmt.Errorf`或`_ =`，SQL 静态检索确认无`ON DUPLICATE`、`AUTO_INCREMENT`、`SERIAL`、`DROP TABLE`、裸`TIMESTAMP`或显式自增`id`写入；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `3.9`：已系统审查市场服务错误、日志和初始化边界。业务错误码集中定义在`marketplace_code.go`，全部使用`bizerr.MustDefine`声明稳定`PLUGIN_MARKETPLACE_*`机器码、英文 fallback 和 GoFrame 语义码；默认`messageKey`由`bizerr.MessageKey`从错误码派生为`error.plugin.marketplace.*`，并由插件自身`manifest/i18n/en-US/plugin.json`和`manifest/i18n/zh-CN/plugin.json`覆盖。所有可进入 API 或插件调用端的业务错误均通过`bizerr.NewCode`或`bizerr.WrapCode`返回；源码包、动态包路径校验中的底层`gerror.New`仅作为内部诊断 cause，并在返回上传扫描边界前包装为市场包校验业务错误。当前市场服务没有主动日志输出，也没有使用`context.Background()`、`context.TODO()`或直接`g.Log()`；后续如新增日志必须继续传递调用链`ctx`并使用项目`logger`组件。
- `3.9`初始化与`DI`记录：`marketplace.New()`当前不接收也不创建运行期依赖，未临时构造缓存、权限、对象存储、`i18n`、审计或其他关键服务独立实例；若后续 controller、存储、权限或缓存接入本服务，必须改为构造函数显式逐项注入。`backend/plugin.go`的`Register`在声明缺失时返回`error`，`init()`中的`panic(gerror.Wrap(...))`仅限静态源码插件注册边界，用于重复或非法内置注册时阻断宿主启动，符合初始化错误由顶层边界决定的约束。缓存一致性无新增运行期缓存实现；数据权限无新增查询路径，继续沿用`3.7`和`3.8`的查询阶段过滤与下载权限校验；开发工具跨平台无影响，未修改`Makefile`、`linactl`或脚本；API 文档源文本无新增变更，`apidoc`资源无新增缺口。
- `3.9`验证：已新增`marketplace_code_test.go`覆盖全部市场错误码的`PLUGIN_MARKETPLACE_*`前缀、`error.plugin.marketplace.*`消息键、英文 fallback 与`en-US`运行时资源一致、`zh-CN`运行时翻译存在；已补充`backend/plugin_test.go`覆盖`Register(nil)`返回错误。已运行`gofmt`格式化新增测试；已运行`GOWORK=off go test ./backend/internal/service/marketplace -count=1`和`GOWORK=off go test ./... -count=1`覆盖市场服务、源码插件注册和生成包编译。已运行`jq empty manifest/i18n/en-US/plugin.json manifest/i18n/zh-CN/plugin.json manifest/i18n/en-US/apidoc/plugin-marketplace.json manifest/i18n/zh-CN/apidoc/plugin-marketplace.json`验证多语言 JSON；已静态检索确认市场服务无`errors.New`、`fmt.Errorf`、`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`或`_ =`，并确认市场服务和注册入口无`context.Background()`、`context.TODO()`、直接`g.Log()`或丢失上下文的日志调用。
- `4.1`：已在市场源码插件目录新增`frontend/types/marketplace.ts`和`frontend/api/marketplace.ts`，保持前端 API 适配层闭环在`apps/lina-plugins/linapro-plugin-marketplace/`内，未向宿主`apps/lina-vben/apps/web-antd/src/api/system/plugin`混入市场业务。适配层通过宿主已有`pluginApiPath('linapro-plugin-marketplace', ...)`生成`/x/linapro-plugin-marketplace/api/v1/market/...`路径，覆盖市场目录、详情、版本、文档、风险、发布者、插件草稿、版本上传、提交审核、审核、状态更新、下载会话创建、下载会话读取和会话 Blob 下载入口；分页接口统一投影为`{ items, total }`以适配后续`useVbenVxeGrid`代理。
- `4.1`类型与边界记录：前端类型定义按后端 DTO 维护插件类型、市场状态、审核状态、可见性、产物类型、风险类型、风险等级、下载会话状态、发布者、插件列表项、插件详情、版本、文档、风险、发布表单、审核表单、下载会话等结构；上传接口使用`FormData`和`multipart/form-data`，下载接口使用`requestClient.download`。本任务不新增页面、路由、菜单、按钮、表格、表单或用户可见文案，不调用`$t()`，因此`i18n`运行时资源无新增影响，E2E 质量审查未触发；API 契约未改后端 DTO，仅按既有后端契约建立前端调用薄封装。缓存一致性、数据权限和`DI`无新增运行期影响，权限仍以后端接口和后续页面权限控制为准；开发工具跨平台无影响，未修改构建脚本、`Makefile`、`linactl`或 Vben 配置。
- `4.1`验证：已运行静态检索确认市场前端适配层无模块级`$t()`调用、无`any`类型、无`console`、`document`或`window`直接依赖；已静态确认适配层通过`#/api/request`复用`pluginApiPath`和`requestClient`，没有硬编码宿主普通插件治理 API。已尝试运行`pnpm --dir apps/lina-vben/apps/web-antd typecheck`，当前环境因`apps/lina-vben`和项目根均缺少`node_modules`导致`vue-tsc: command not found`而无法执行；该阻断已记录，后续`5.6`在依赖可用后仍需运行完整前端类型检查、相关测试和`i18n`治理验证。已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `4.2`：已新增`apps/lina-plugins/linapro-plugin-marketplace/frontend/pages/catalog/index.vue`市场目录页，使用插件页面装配机制注册`plugin-marketplace-catalog`路由，页面实现使用`Page`和`useVbenVxeGrid`，市场列表通过插件自身`marketplacePluginList`适配器远程分页加载。搜索条件覆盖关键词、插件类型、标签、发布者和兼容宿主版本；列表展示插件名称、插件 ID、摘要、插件类型、发布者认证标记、最新版本、宿主兼容范围、标签、风险摘要、下载统计和更新时间。
- `4.2`i18n与前端边界记录：已在市场插件自己的`manifest/i18n/en-US/plugin.json`和`manifest/i18n/zh-CN/plugin.json`维护目录页运行时文案，未修改宿主语言包。页面没有在模块顶层直接调用`$t()`求值，表单 schema、插件类型选项和表格列定义均封装为函数并在组件挂载后写入`Grid`，模板和运行时 helper 再读取翻译资源。市场前端仍闭环在`apps/lina-plugins/linapro-plugin-marketplace/`目录内，未修改`apps/lina-core`稳定插件能力、宿主普通插件管理 API 或`apps/lina-vben`路由装配代码。数据权限继续由后端列表接口查询阶段过滤保障，前端不新增权限绕行；缓存一致性无新增运行期缓存；`DI`无新增运行期依赖；开发工具跨平台无影响，未修改`Makefile`、`linactl`或构建脚本。
- `4.2`验证：已运行`jq empty`验证市场插件中英文运行时语言包和`apidoc`占位资源均为合法 JSON；已运行静态检索确认市场插件前端无显式 TypeScript `any`、无`console`、`document`或`window`直接依赖；已运行自定义静态检查确认目录页引用的`26`个`catalog`运行时翻译 key 在`en-US`与`zh-CN`资源中均有覆盖，并确认`<script setup>`中不存在顶层`$t()`调用；已检查目录页相对导入目标存在。已尝试运行`pnpm --dir apps/lina-vben/apps/web-antd typecheck`，当前仍因`apps/lina-vben`缺少`node_modules`导致`vue-tsc: command not found`而无法执行；该完整前端类型检查仍由后续`5.6`在依赖可用后执行。目录页涉及用户可观察行为，E2E 覆盖按既定任务`6.1`补充`TC001-marketplace-catalog.ts`，本任务先以静态检查、i18n 覆盖检查和`openspec validate add-plugin-marketplace --strict`作为当前可用验证；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `4.3`：已新增`apps/lina-plugins/linapro-plugin-marketplace/frontend/pages/detail/index.vue`插件详情页，并在目录页增加详情操作入口。详情页使用插件页面装配机制注册`plugin-marketplace-detail`隐藏路由，展示插件基础信息、版本列表、文档浏览、风险摘要和下载确认流程；版本列表使用`useVbenVxeGrid`远程分页加载，文档与风险根据选中版本调用插件自身 API 读取。`plugin.yaml`已补充可见的市场目录菜单和隐藏详情菜单，组件均指向宿主既有`system/plugin/dynamic-page`插件页承载入口，菜单名称使用插件运行时 i18n key。
- `4.3`下载与边界记录：详情页下载流程先创建短期下载会话，再通过`requestClient.download`获取受控 Blob 并调用宿主既有`downloadBlob`保存文件。源码插件下载确认文案明确要求放入`apps/lina-plugins/`后重新构建宿主，不展示或调用运行时一键安装；动态插件下载确认文案明确仍需走现有本地动态插件上传治理。市场详情页不绕过后端可见性和下载权限，所有数据仍通过市场插件后端查询和下载会话接口返回；未修改`apps/lina-core`插件安装、启停、升级或动态上传公共契约。
- `4.3`i18n与治理记录：已在市场插件自己的`manifest/i18n/en-US/plugin.json`和`manifest/i18n/zh-CN/plugin.json`补齐详情、版本、文档、风险、下载确认、状态、审核状态、可见性、产物类型、风险类型、风险等级和菜单运行时文案；未修改宿主语言包。页面没有在模块顶层直接调用`$t()`求值，版本表列定义在组件挂载后写入`Grid`，状态枚举均通过显式函数映射到运行时语言资源。缓存一致性无新增运行期缓存；`DI`无新增运行期依赖；开发工具跨平台无影响，未修改`Makefile`、`linactl`或构建脚本。
- `4.3`验证：已运行`jq empty`验证市场插件中英文运行时语言包和`apidoc`占位资源均为合法 JSON；已运行`ruby -e 'require "yaml"; ...'`解析`plugin.yaml`并确认存在`2`个菜单；已运行自定义静态检查确认目录页和详情页引用的`95`个`catalog/detail`运行时翻译 key 在`en-US`与`zh-CN`资源中均有覆盖，且`plugin.yaml`中的`2`个菜单 i18n key 双语覆盖；已运行静态检查确认市场插件页面`<script setup>`中不存在顶层`$t()`调用，前端无显式 TypeScript `any`、无`console`、`document`或`window`直接依赖，并确认相对导入目标存在。已尝试运行`pnpm --dir apps/lina-vben/apps/web-antd typecheck`，当前仍因`apps/lina-vben`缺少`node_modules`导致`vue-tsc: command not found`而无法执行；完整前端类型检查、相关测试和`i18n`治理验证仍由后续`5.6`执行。详情、文档、风险和下载确认属于用户可观察流程，E2E 覆盖按既定任务`6.1`和`6.2`补充；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `4.4`：已新增`apps/lina-plugins/linapro-plugin-marketplace/frontend/pages/console/index.vue`发布和审核控制台，并在`plugin.yaml`新增`marketplace-console`菜单，继续通过宿主既有`system/plugin/dynamic-page`承载插件页面。发布页使用`Page`、`useVbenForm`和`Upload.Dragger`，覆盖发布者创建、插件草稿创建、源码/动态`.zip`草稿上传、替换草稿开关、提交审核和最新草稿审核结果查看；审核页使用`useVbenVxeGrid`按插件 ID 加载版本列表，支持选择版本后查看清单产物、文档入口、`SQL`、`hostServices`和风险计数，并通过审核决策表单提交通过或拒绝。
- `4.4`治理记录：本任务继续闭环在`apps/lina-plugins/linapro-plugin-marketplace/`源码插件目录内，未修改`apps/lina-core`稳定插件能力、普通插件管理 API、动态插件上传后端契约或宿主路由发现机制。数据权限影响由后端发布、审核、详情、文档和风险接口继续校验，前端不新增权限绕行或本地过滤作为安全依据；缓存一致性无新增运行期缓存；`DI`来源检查结论为无新增运行期服务依赖；开发工具跨平台无影响，未修改`Makefile`、`linactl`、构建脚本或 Vben 配置。`i18n`有影响，已在市场插件自己的`manifest/i18n/en-US/plugin.json`和`manifest/i18n/zh-CN/plugin.json`维护控制台菜单、标签页、表单、按钮、上传提示、审核卡片、空状态和消息文案；`apidoc`资源无新增源文本变更。
- `4.4`验证：已运行`jq empty`验证市场插件中英文运行时语言包和`apidoc`占位资源均为合法 JSON；已运行`ruby -e 'require "yaml"; ...'`解析`plugin.yaml`并确认`type=source`、`distribution=builtin`、`i18n.enabled=true`且存在`3`个菜单；已运行自定义静态检查确认目录页、详情页、控制台页和`plugin.yaml`引用的`143`个运行时翻译 key 在`en-US`与`zh-CN`资源中均有覆盖，并确认市场插件页面`<script setup>`中不存在顶层`$t()`调用；已运行精确静态扫描确认市场插件前端无显式 TypeScript `any`、无`console`、`document`或`window`直接依赖。已尝试运行`pnpm --dir apps/lina-vben/apps/web-antd typecheck`，当前仍因`apps/lina-vben`缺少`node_modules`导致`vue-tsc: command not found`而无法执行；已尝试运行`pnpm --dir apps/lina-vben/apps/web-antd exec eslint .../console/index.vue`，当前同样因依赖缺失导致`eslint`命令不可用；完整前端类型检查、相关测试和`i18n`治理验证仍由后续`5.6`在依赖可用后执行。发布、上传和审核控制台属于用户可观察流程，E2E 覆盖按既定任务`6.2`和`6.3`补充；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `4.5`：已在`apps/lina-plugins/linapro-plugin-marketplace/frontend/pages/detail/index.vue`扩展动态插件下载衔接。动态版本在用户具备`plugin:install`权限时，确认按钮显示为“下载并导入”，流程先通过市场下载会话获取动态市场包 Blob，再在浏览器侧读取 ZIP 中根目录或单一插件根目录下的`plugin.wasm`，校验 WASM 魔数后调用宿主既有`pluginDynamicUpload(file, false)`入口，并通过`notifyPluginRegistryChanged()`通知插件注册表变化；本地宿主仍执行动态上传治理中的 WASM 文件头、ABI、内嵌清单、SQL、依赖和`hostServices`校验。若用户没有`plugin:install`权限，动态包仍可按市场下载权限下载，但页面明确提示不能导入本地治理。源码插件仍只展示源码交付边界并下载源码包，不展示、不调用运行时一键安装。
- `4.5`治理记录：本任务只修改市场源码插件前端和插件自身运行时语言包，未修改`apps/lina-core`动态上传后端、普通插件管理页面或宿主公共契约；对宿主能力的复用限于既有公开前端 API`#/api/system/plugin`中的`pluginDynamicUpload`。数据权限影响继续由市场下载会话和宿主动态上传接口分别校验，前端仅做权限提示和导入衔接；缓存一致性无新增运行期缓存，动态上传成功后只触发插件前端注册表变更通知；`DI`来源检查结论为无新增运行期服务依赖；开发工具跨平台无影响，未修改脚本、`Makefile`、`linactl`或构建配置。`i18n`有影响，已在市场插件自己的中英文运行时语言包维护动态导入按钮、边界、权限提示、导入成功和`plugin.wasm`提取失败文案；`apidoc`资源无新增源文本变更。
- `4.5`验证：已运行`jq empty`验证市场插件中英文运行时语言包和`apidoc`占位资源均为合法 JSON；已运行自定义静态检查确认目录页、详情页、控制台页和`plugin.yaml`引用的`150`个运行时翻译 key 在`en-US`与`zh-CN`资源中均有覆盖，并确认市场插件页面`<script setup>`中不存在顶层`$t()`调用；已运行精确静态扫描确认市场插件前端无显式 TypeScript `any`、无`console`、`document`或`window`直接依赖。已尝试运行`pnpm --dir apps/lina-vben/apps/web-antd typecheck`，当前仍因`apps/lina-vben`缺少`node_modules`导致`vue-tsc: command not found`而无法执行；完整前端类型检查、相关测试和`i18n`治理验证仍由后续`5.6`在依赖可用后执行。动态下载导入属于用户可观察流程，E2E 覆盖按既定任务`6.2`补充；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `4.6`：已完成市场插件前端与后端 i18n 治理复核。`plugin.yaml`保持`i18n.enabled: true`、`default: en-US`和`en-US`/`zh-CN`语言声明，`marketplace-catalog`、`marketplace-detail`和`marketplace-console`菜单名称均使用插件运行时 i18n key；市场目录、详情、发布审核控制台、下载导入、状态、审核状态、可见性、产物类型、风险类型、风险等级、错误消息和空状态文案均由市场插件自己的`manifest/i18n/<locale>/plugin.json`维护。接口文档翻译继续隔离在市场插件自己的`manifest/i18n/<locale>/apidoc/plugin-marketplace.json`，`en-US`保持空对象占位，`zh-CN`覆盖非英文接口文档翻译。
- `4.6`治理记录：前端页面的表单 schema、表格列定义、选项列表和枚举标签均通过函数在运行期求值，未在`.vue`的`<script setup>`顶层直接调用`$t()`。本任务未新增后端 API 源文本、错误码或运行期服务依赖；`DI`来源检查结论为无新增运行期依赖。数据权限、缓存一致性、开发工具跨平台和 SQL 均无新增影响；用户可观察文案变化已纳入插件运行时中英文语言包，E2E 对关键文案不显示原始 key 的覆盖按既定任务`6.3`补充。
- `4.6`验证：已运行`jq empty`验证市场插件运行时语言包和`apidoc`资源均为合法 JSON；已运行运行时语言包 key 对齐检查，确认`en-US`与`zh-CN`运行时语言包各`177`个 key 完全一致；已运行菜单覆盖检查，确认`plugin.yaml`中的`3`个菜单 i18n key 双语覆盖；已运行页面 key 覆盖检查，确认市场插件页面引用的`147`个插件运行时 key 均双语覆盖；已运行顶层`$t()`静态检查，确认市场插件页面`<script setup>`顶层无直接`$t()`调用；已运行精确静态扫描确认市场插件前端无显式 TypeScript`any`、无`console`、`document`或`window`直接依赖；已运行`GOWORK=off go test ./backend/internal/service/marketplace -run 'TestMarketplace.*Code|Test.*Error' -count=1`验证市场错误码与运行时翻译资源；已运行`GOWORK=/Users/john/Workspace/github/linaproai/linapro-site/temp/go.work.plugins GOFLAGS=-tags=official_plugins go test ./internal/service/apidoc -run 'TestOpenAPIMetadataUsesEnglishSourceText|TestOpenAPII18nBundlesCoverCurrentMetadata|TestOpenAPIBundlesAreSeparatedFromRuntimeI18n' -count=1`验证英文源文本、插件`apidoc`翻译完整性和运行时语言包隔离。已尝试运行`pnpm --dir apps/lina-vben/apps/web-antd typecheck`，当前仍因`apps/lina-vben`缺少`node_modules`导致`vue-tsc: command not found`而无法执行；完整前端类型检查、相关测试和`make i18n.check`或等价治理验证仍由后续`5.6`在依赖可用后执行。已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `5.1`：已补齐后端单元测试，覆盖源码市场包缺少`backend/plugin.go`、`frontend/`、`plugin_embed.go`和`manifest/docs/**/*.md`时拒绝；覆盖动态运行时包携带`backend/`、`frontend/`、`hack/`或`main.go`开发源码入口时拒绝；覆盖根级`plugin.yaml`与内嵌清单不一致；覆盖草稿和拒绝版本可替换、已提交、审核中、已批准、已发布、已下架和已废弃版本不可覆盖。
- `5.1`治理记录：本任务只新增或补充市场插件后端单元测试，未修改生产代码、SQL、API DTO、前端页面、运行时语言包、构建脚本或宿主公共契约；`DI`来源检查结论为无新增运行期依赖，缓存一致性、数据权限、开发工具跨平台、SQL 幂等性和`i18n`均无新增运行期影响。测试均在函数内部构造 ZIP 包、WASM 段、实体状态和临时目录，使用`t.TempDir()`闭环清理，不依赖执行顺序或数据库残留。
- `5.1`验证：已运行`GOWORK=off go test ./backend/internal/service/marketplace -run 'TestScanSourcePackage|TestScanDynamicPackage|TestImmutableRelease' -count=1`覆盖本任务新增和相关后端测试；已运行`GOWORK=off go test ./... -count=1`覆盖市场插件根包、`backend`包、API 包、生成 DAO/DO/Entity 包和市场服务包编译；已静态检索确认市场服务代码无`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`、`errors.New`、`fmt.Errorf`或`_ =`；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `5.2`：已补齐后端单元测试，覆盖`manifest/docs/`文档脚本标签转义、安全渲染、越界图片路径拒绝、按插件默认语言回退、`README`回退和文档渲染缓存键。新增`documentRenderCacheKey`纯函数将未来渲染缓存键固定为`marketplace:document-render:v1:<release_id>:<locale>:<escaped_path>:<content_hash>`，测试确认`release_id`、语言、路径或内容 hash 任一变化都会生成不同 key。
- `5.2`治理记录：本任务仅新增文档缓存键纯函数和对应后端测试，未引入实际缓存实例、缓存后端、订阅状态或跨实例协调依赖；`DI`来源检查结论为无新增运行期依赖。缓存一致性边界为：权威数据源仍是版本产物和`plugin_marketplace_doc.content_hash`索引，缓存键包含版本发布 ID、语言、文档路径和内容 hash，新版本发布或草稿产物替换会因`release_id`或`content_hash`变化自然隔离旧渲染结果；当前没有运行期缓存失效事件、跨实例同步或最大陈旧窗口需要实现。数据权限、SQL、前端 UI、运行时`i18n`和开发工具跨平台无新增影响。
- `5.2`验证：已运行`gofmt`格式化文档服务与测试；已运行`GOWORK=off go test ./backend/internal/service/marketplace -run 'TestIndexMarketplaceDocument|TestSelectMarketplaceDocumentFallback|TestDocumentRenderCacheKey' -count=1`覆盖本任务文档测试；已运行`GOWORK=off go test ./... -count=1`覆盖市场插件根包、`backend`包、API 包、生成 DAO/DO/Entity 包和市场服务包编译；已静态检索确认市场服务代码无`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`、`errors.New`、`fmt.Errorf`或`_ =`；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `5.3`：已补齐市场列表、可见性和下载统计后端单元测试。列表测试覆盖分页默认值和最大`pageSize`上限、发布态读模型基础条件、读模型标签、风险计数、发布者快照和下载计数投影；可见性测试覆盖零主体仅公共可见、主体 scope 去重、grant 条件构造、`download`权限条件和插件/版本限制性可见性合并；下载测试覆盖 TTL 上限、受控下载 URL、已消费会话不返回 URL、过期会话不可用、下载主体补充请求人 scope、授权快照 JSON、有效产物类型和统计快照同时写入插件主表与列表读模型的 DO 数据。
- `5.3`治理记录：本任务只新增或重构市场插件后端纯 helper 和单元测试，未新增 SQL、API DTO、前端 UI、运行时语言包、构建脚本或宿主公共契约；`DI`来源检查结论为无新增运行期依赖。数据权限影响已通过数据库查询前的可见性条件构造、`download`权限条件和下载主体 scope 测试覆盖，真实查询路径仍在`ListPlugins`、详情、文档、风险和下载会话创建阶段调用统一可见性过滤；聚合统计只从`completed`下载事件刷新插件主表和读模型快照，不作为权限依据。缓存一致性、SQL 幂等性、运行时`i18n`和开发工具跨平台无新增影响；测试不依赖真实数据库、执行顺序或残留数据。
- `5.3`验证：已运行`gofmt`格式化查询、可见性、下载服务与测试；已运行`GOWORK=off go test ./backend/internal/service/marketplace -run 'TestNormalizeMarketplacePageBounds|TestPluginListReadModelBaseCriteriaRequiresPublished|TestPluginListItemFromReadModelDecodesSnapshots|TestMarketplaceVisibility|TestDownloadSessionActiveAndNotExpired|TestDownloadVisibilitySubjectAddsRequesterScope|TestDownloadStatisticsSnapshotData|TestNormalizeDownloadSessionTTLBounds' -count=1`覆盖本任务测试；已运行`GOWORK=off go test ./... -count=1`覆盖市场插件根包、`backend`包、API 包、生成 DAO/DO/Entity 包和市场服务包编译；已静态检索确认市场服务代码无`g.Map`、`map[string]interface{}`、`map[string]any`、`WhereNull`、`errors.New`、`fmt.Errorf`或`_ =`；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `5.4`：已新增`apps/lina-plugins/linapro-plugin-marketplace/hack/tests/unit/marketplace-frontend.test.mjs`，使用 Node 标准库`node:test`覆盖市场前端 API 适配、目录页分页与筛选组合逻辑、发布审核控制台组合逻辑、插件运行时 i18n key 双语覆盖，以及动态市场包下载后提取`plugin.wasm`并调用宿主`pluginDynamicUpload`和`notifyPluginRegistryChanged`的静态衔接。
- `5.4`治理记录：本任务只新增插件自有前端测试资产，未修改页面运行时行为、后端 API、SQL、宿主公共契约、运行时语言包或构建入口；`DI`来源检查结论为无新增运行期依赖。开发工具跨平台影响为新增`.mjs`测试文件，该测试仅使用 Node 标准库`node:test`、`fs`和`path`，通过`node --test <file>`运行，不依赖 POSIX shell、平台路径分隔符或 Vben `node_modules`。`i18n`影响为测试覆盖运行时 key 双语资源；数据权限、缓存一致性和 SQL 无新增影响。用户可观察流程的浏览器级断言仍按后续`6.x` E2E 任务补充。
- `5.4`验证：已运行`node --test apps/lina-plugins/linapro-plugin-marketplace/hack/tests/unit/marketplace-frontend.test.mjs`，`5`个测试全部通过；已运行精确静态扫描确认市场前端和新增测试无显式 TypeScript`any`、无真实`console.*`、`document.*`或`window.*`调用；已运行`openspec validate add-plugin-marketplace --strict`并通过。完整 Vben 类型检查和`make i18n.check`或等价前端治理验证仍由`5.6`执行，当前环境缺少`apps/lina-vben`的`node_modules`，不能用 Vitest/Vue Test Utils 执行组件级测试。
- `5.5`：已运行市场插件后端门禁测试，覆盖市场服务包、源码插件后端注册包和插件模块全量编译。当前市场插件尚未新增`backend/internal/controller`或源码插件路由绑定，`backend/plugin.go`只负责静态源码插件注册和嵌入资源声明，因此“涉及路由绑定时运行插件或宿主启动绑定测试”判定为本任务无直接路由绑定影响。
- `5.5`治理记录：为满足后端 panic 治理，已在`apps/lina-core/internal/cmd/cmd_test.go`的生产 panic allowlist 中补充`apps/lina-plugins/linapro-plugin-marketplace/backend/plugin.go:init`两处静态注册 panic，原因限定为内置源码插件静态注册在错误返回 API 失败后 fail-fast。该修改只更新宿主治理测试 allowlist，不修改`lina-core`运行时插件公共契约、服务语义、SQL、API DTO 或前端行为；`DI`来源检查结论为无新增运行期依赖。数据权限、缓存一致性、运行时`i18n`和开发工具跨平台无新增影响。
- `5.5`验证：已运行`GOWORK=off go test ./backend/internal/service/marketplace -count=1`、`GOWORK=off go test ./backend -count=1`和`GOWORK=off go test ./... -count=1`并通过。已运行`GOWORK=/Users/john/Workspace/github/linaproai/linapro-site/temp/go.work.plugins GOFLAGS=-tags=official_plugins go test ./internal/cmd -run 'TestRequireCommandConfirmation|TestCommandPackageHasNoHanText|TestParseInitRebuildFlag|TestSourcePluginProtectedRoutesIncludeTenancy' -count=1`并通过，覆盖宿主命令包编译和源码插件受保护路由静态治理子集。尝试运行完整`go test ./internal/cmd -count=1`时，市场插件 panic allowlist 已不再报未授权 panic，但当前本地`apps/lina-plugins/`目录只包含`linapro-plugin-marketplace`，导致既有旧官方插件 allowlist 报 stale，完整宿主命令包测试需在官方插件目录完整恢复后重跑；该阻断不来自市场插件新增代码。已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `5.6`：已完成前端类型检查、相关测试和 i18n 治理验证。为解除此前`vue-tsc: command not found`阻断，已在`apps/lina-vben`执行`pnpm install --frozen-lockfile`安装依赖，`git status`确认未产生`apps/lina-vben`源码或 lockfile 变更，仅生成被忽略的依赖目录。
- `5.6`治理记录：本任务不新增生产代码、SQL、后端 API、运行期服务依赖或宿主公共契约；`DI`来源检查结论为无新增运行期依赖。`i18n`治理通过 Vben 自带`i18n:check`和插件前端 Node 单测共同覆盖，插件运行时 key 双语资源对齐。开发工具跨平台影响仅限使用既有`pnpm`和 Node 标准测试入口，未新增构建脚本、`Makefile`或`linactl`命令。数据权限、缓存一致性和 SQL 无新增影响；用户可观察流程仍按后续`6.x` E2E 任务验证。
- `5.6`验证：已运行`pnpm --dir apps/lina-vben install --frozen-lockfile`并完成依赖安装；已运行`pnpm --dir apps/lina-vben/apps/web-antd typecheck`并通过；已运行`pnpm --dir apps/lina-vben/apps/web-antd i18n:check`并通过，输出`Validated 4 frontend locale files across 2 locales.`；已运行`node --test apps/lina-plugins/linapro-plugin-marketplace/hack/tests/unit/marketplace-frontend.test.mjs`并通过`5`个测试；已运行`pnpm --dir apps/lina-vben exec prettier --check ../lina-plugins/linapro-plugin-marketplace/hack/tests/unit/marketplace-frontend.test.mjs`并通过；已运行精确静态扫描确认市场前端和新增测试无显式 TypeScript`any`、无真实`console.*`、`document.*`或`window.*`调用；已运行`openspec validate add-plugin-marketplace --strict`并通过。
- `6.1`：已创建`apps/lina-plugins/linapro-plugin-marketplace/hack/tests/e2e/TC001-marketplace-catalog.ts`，使用插件自有 POM 覆盖市场目录加载、关键词筛选、类型筛选、详情跳转、版本文档页和风险摘要页；用例通过中文文案、表格行、版本号、文档标题和风险摘要断言证明目录、详情、文档和风险工作流可用。
- `6.2`：已创建`apps/lina-plugins/linapro-plugin-marketplace/hack/tests/e2e/TC002-publish-review-download.ts`，覆盖发布者资料保存、源码插件草稿保存、源码包上传和提交审核、动态包上传和提交审核、审核后台检查和通过、源码包下载只走源码交付，以及动态包下载后提取`plugin.wasm`并调用本地动态上传治理；断言下载请求、上传请求、审核请求和动态上传调用次数，避免只验证按钮存在。
- `6.3`：已创建`apps/lina-plugins/linapro-plugin-marketplace/hack/tests/e2e/TC003-visibility-i18n.ts`，覆盖私有插件在列表和总数中不可见、下载权限拒绝只记录一次下载请求且不触发动态上传，并分别在`zh-CN`与`en-US`下断言关键市场目录、类型和操作文案显示为翻译文本且不出现原始`plugin.linapro-plugin-marketplace`或`error.plugin.marketplace`key。
- `6.4`：已将市场插件专属`MarketplacePage`页面对象放入`apps/lina-plugins/linapro-plugin-marketplace/hack/tests/pages/`，将市场 API mock、运行时 i18n mock、菜单路由 mock、截图 helper 和工作台打开 helper 放入`apps/lina-plugins/linapro-plugin-marketplace/hack/tests/support/marketplace-fixtures.ts`；未向宿主`hack/tests/pages/`或`hack/tests/support/`新增市场插件专属定位器。POM 已补强`Vben`表单字段定位、`VXE`固定操作列定位、Ant Select 当前值短路和`source/dynamic`中英文别名匹配；mock 数据每次`installMarketplaceApiMocks`创建隔离版本 Map，避免上传审核状态在测试文件之间泄漏。
- `6.5`验证：已在当前工作区启动`LINAPRO_SOURCE_PLUGINS=1 pnpm --dir apps/lina-vben/apps/web-antd exec vite --mode development --host 127.0.0.1 --port 5667 --strictPort`，并以`E2E_BASE_URL=http://127.0.0.1:5667`执行市场插件 E2E。最终命令`pnpm --dir hack/tests exec playwright test -c playwright.config.ts apps/lina-plugins/linapro-plugin-marketplace/hack/tests/e2e/TC001-marketplace-catalog.ts apps/lina-plugins/linapro-plugin-marketplace/hack/tests/e2e/TC002-publish-review-download.ts apps/lina-plugins/linapro-plugin-marketplace/hack/tests/e2e/TC003-visibility-i18n.ts`通过`3 passed (40.2s)`；截图、trace 和测试产物写入`temp/e2e/test-results/`。已抽查`2026-07-10T03-11-27-zh-catalog.png`、`2026-07-10T03-11-30-download-denied.png`和`2026-07-10T03-11-33-en-catalog.png`，确认关键文案为目标语言翻译、下载拒绝显示`插件市场下载会话不存在`、无原始 i18n key、无明显重叠或空白页面。
- `6.x`补充验证：已运行`pnpm --dir hack/tests exec tsc -p tsconfig.json --noEmit`、`pnpm --dir apps/lina-vben/apps/web-antd typecheck`、`pnpm --dir apps/lina-vben/apps/web-antd i18n:check`、`node --test apps/lina-plugins/linapro-plugin-marketplace/hack/tests/unit/marketplace-frontend.test.mjs`和`pnpm --dir apps/lina-vben exec prettier --check ...`并通过。为下载拒绝路径补充详情页局部错误处理，使用后端`messageKey`和插件运行时语言包显示本地化错误；请求层调用设置`silentErrorMessage`避免重复 toast。该修复不新增文案 key，运行时翻译沿用既有`error.plugin.marketplace.download.*`资源。
- `7.1`：已运行`openspec validate add-plugin-marketplace --strict`，结果为`Change 'add-plugin-marketplace' is valid`。
- `7.2`治理记录：架构边界保持市场业务闭环在`apps/lina-plugins/linapro-plugin-marketplace/`源码插件内，本轮只修改插件前端、插件 E2E、插件 POM/helper 和插件前端单测，未新增或修改`apps/lina-core`稳定插件公开契约；数据权限由后端列表、详情、文档、风险和下载会话查询阶段可见性过滤以及 E2E 私有插件不可见、下载拒绝断言覆盖；缓存一致性无新增运行期缓存服务，读模型权威源、文档 hash 键和下载统计快照边界沿既有实现记录，权限不依赖缓存放行；`i18n`有影响且已通过插件运行时双语资源、`i18n:check`、E2E 中英文关键文案和无原始 key 断言验证；开发工具跨平台未新增`Makefile`、`linactl`或脚本，测试 helper 使用`Node`/`Playwright`跨平台 API，未新增平台专属入口；SQL 幂等性无本轮新增变更，既有插件 SQL 仍为`PostgreSQL`幂等 DDL/Seed DML 且已在前序任务验证；测试策略覆盖后端单测、前端 Node 单测、类型检查、格式检查、i18n 检查和三条插件 E2E；`DI`来源检查结论为本轮前端和 E2E 修复无新增运行期服务依赖，既有后端市场服务`New()`仍未临时创建缓存、权限、对象存储、`i18n`或审计服务实例。
- `7.3`：已读取`apps/lina-core/pkg/plugin/README.md`和`apps/lina-core/pkg/plugin/README.zh-CN.md`，两份文档已经明确`distribution=builtin`是仅适用于源码插件的治理模式，且本次没有变更宿主插件公开契约、`capability.Services`、`pluginhost`、`pluginbridge`或动态`host service`描述符；因此确认`apps/lina-core/pkg/plugin`README 无需同步更新。
- `7.4`：已按`lina-review`完成代码与规范审查，范围来源为`git status --short`、`git diff --name-only`、`git diff --cached --name-only`和`git ls-files --others --exclude-standard`展开，覆盖`.gitmodules`删除、`apps/lina-core/internal/cmd/cmd_test.go`、`apps/lina-core/pkg/dialect/dialect_postgres_assets_test.go`、`apps/lina-plugins`从`gitlink`转普通目录、`apps/lina-plugins/linapro-plugin-marketplace/`全部新增文件以及`openspec/changes/add-plugin-marketplace/`文档，共`113`个审查候选项；插件根`apps/lina-plugins/linapro-plugin-marketplace/AGENTS.md`不存在。已读取`AGENTS.md`、`openspec.md`、`documentation.md`、`markdown-format.instructions.md`、`architecture.md`、`plugin.md`、`testing.md`、`backend-go.md`、`api-contract.md`、`database.md`、`data-permission.md`、`cache-consistency.md`、`i18n.md`、`frontend-ui.md`和`dev-tooling.md`，并按`goframe-v2`和`lina-e2e`要求审查后端与 E2E。审查结论为未发现阻塞问题，严重`0`、警告`0`：市场实现保持在内置源码插件目录内，`plugin.yaml`满足`type: source`、`distribution: builtin`和`i18n.enabled: true`；后端 API 使用分页、Unix 毫秒时间字段、权限标签和可见性查询阶段过滤；数据库 SQL 为插件自有`PostgreSQL`幂等 DDL/Seed DML，未发现裸`TIMESTAMP`、`ON DUPLICATE`或显式自增`id`写入；缓存仅使用读模型、文档 hash 键和下载统计快照边界，权限不依赖缓存放行；前端 API、页面、POM、mock 和 E2E 均闭环在插件目录，关键中英文文案和下载拒绝路径已覆盖，无原始`i18n`key；开发工具无新增跨平台脚本入口。复验命令已通过：`openspec validate add-plugin-marketplace --strict`、`GOWORK=off go test ./... -count=1`、`go test ./pkg/dialect -run TestPostgreSQLProjectSQLUsesTimestamptzForInstants -count=1`、`GOWORK=/Users/john/Workspace/github/linaproai/linapro-site/temp/go.work.plugins GOFLAGS=-tags=official_plugins go test ./internal/cmd -run 'TestRequireCommandConfirmation|TestCommandPackageHasNoHanText|TestParseInitRebuildFlag|TestSourcePluginProtectedRoutesIncludeTenancy' -count=1`、`pnpm --dir hack/tests exec tsc -p tsconfig.json --noEmit`、`pnpm --dir apps/lina-vben/apps/web-antd typecheck`、`pnpm --dir apps/lina-vben/apps/web-antd i18n:check`、`node --test apps/lina-plugins/linapro-plugin-marketplace/hack/tests/unit/marketplace-frontend.test.mjs`、`pnpm --dir apps/lina-vben exec prettier --check ...`和市场插件三条 E2E，结果`3 passed (37.8s)`。已抽查最新截图`2026-07-10T03-23-33-zh-catalog.png`、`2026-07-10T03-23-35-download-denied.png`、`2026-07-10T03-23-38-en-catalog.png`和`2026-07-10T03-23-27-dynamic-imported.png`，未见空白页、明显重叠或原始翻译 key。剩余风险：完整`go test ./internal/cmd -count=1`仍依赖旧官方插件目录完整性，当前工作区只包含市场插件时会触发既有旧官方插件 allowlist stale，已用覆盖本次变更的子集测试替代。

## Feedback

- [x] **FB-1**: 补齐 marketplace controller、路由绑定与本地产物存储下载闭环
- [x] **FB-2**: 文档读路径返回真实包内渲染正文，而非仅 searchText 摘要
- [x] **FB-3**: 删除前端 ZIP/WASM 解压实现，改为下载后端 `plugin_wasm` 产物导入
- [x] **FB-4**: 删除未使用的 `documentRenderCacheKey` 预埋死代码
- [x] **FB-5**: 修复 plugin.yaml 菜单 key 未使用 `plugin:<id>:*` 前缀导致 `make dev` 启动失败
- [x] **FB-6**: 补齐市场菜单 parent_key 并抬升版本，触发 builtin 升级同步侧栏入口

### 反馈执行记录

- `FB-1`：已生成并实现 `backend/internal/controller/market`，在 `backend/plugin.go` 通过 `HTTP().RegisterRoutes` 绑定到 `/x/linapro-plugin-marketplace/api/v1`；新增本地 `ArtifactStore`，上传后落盘 package/wasm，下载会话 content 端点可流式读取；新增 `ListPublishers` 与 `OpenDownloadContent`。
- `FB-2`：`GetReleaseDocument` 在索引回退后从产物存储重新打开 ZIP 并渲染安全 HTML；索引 searchText 仅作回退。
- `FB-3`：详情页删除浏览器 ZIP 中央目录/inflate 实现；动态导入改为请求 `plugin_wasm` 产物。
- `FB-4`：删除 `documentRenderCacheKey` 及其单测。
- 验证：`GOWORK=off go test ./... -count=1`（marketplace 插件模块）通过；`node --test apps/lina-plugins/linapro-plugin-marketplace/hack/tests/unit/marketplace-frontend.test.mjs` 通过 5 个用例。
- 影响分析：`i18n` 无新增 key；缓存一致性无运行期缓存；数据权限沿用查询阶段可见性与下载会话校验；开发工具无脚本变更；命中规则 `plugin`/`backend-go`/`api-contract`/`frontend-ui`/`testing`/`data-permission`。
- `FB-5`：根因是宿主 `ValidateManifestMenus` 要求菜单 key 使用 `plugin:<plugin-id>:*` 前缀，但市场插件 `plugin.yaml` 写了裸 key（`marketplace-catalog` 等），导致 `BootstrapBuiltinPlugins` 扫描内置源码插件时失败、`make dev` 退出。已将三个菜单 key 修正为 `plugin:linapro-plugin-marketplace:marketplace-catalog|detail|console`；路由 `path`、前端 `routePath`、E2E 路径和 i18n 菜单名称 key 保持不变。新增前端单测断言 `plugin.yaml` 菜单 key 必须带宿主前缀。
- `FB-5` 验证：`node --test apps/lina-plugins/linapro-plugin-marketplace/hack/tests/unit/marketplace-frontend.test.mjs` 通过 6 个用例；`make stop && make dev`（插件全量模式）成功，Lina Core `http://127.0.0.1:9120/` 与 Lina Vben `http://127.0.0.1:5666/` 均 running。
- `FB-5` 影响分析：修改文件为 `plugin.yaml`、`hack/tests/unit/marketplace-frontend.test.mjs`、`openspec/changes/add-plugin-marketplace/tasks.md`；用户可观察路由 path 未变；`i18n` 无影响（菜单名称仍用既有 `plugin.linapro-plugin-marketplace.menu.*`）；缓存一致性无影响；数据权限无影响；开发工具跨平台无影响；命中规则 `plugin`/`testing`/`openspec`；`frontend-ui`/`backend-go`/`api-contract`/`database`/`data-permission`/`cache-consistency`/`i18n`/`dev-tooling` 本反馈无代码契约变更。
- `FB-6`：根因是市场插件已以`v0.1.0`安装启用后，同版本菜单声明变更不会触发 builtin 升级同步；且菜单缺少`parent_key: extension`。已将版本抬升为`v0.1.1`，三个菜单均设置`parent_key: extension`；新增单测断言父级挂载到扩展中心。
- `FB-6` 验证：`node --test .../marketplace-frontend.test.mjs` 7 passed；`make stop && make dev` 成功；`sys_plugin.version=v0.1.1` 且 enabled；`sys_menu` 已写入 3 条市场菜单且`parent_id`对应扩展中心；`GET /api/v1/user/info`（admin）菜单树包含`plugin-marketplace-catalog/detail/console`，权限包含`market:plugin:*`。
- `FB-6` 影响分析：修改`plugin.yaml`、前端单测、`tasks.md`；用户可观察入口变为扩展中心下可见侧栏；`i18n`无新增 key；缓存/数据权限/开发工具无影响；命中规则`plugin`/`testing`/`openspec`。

